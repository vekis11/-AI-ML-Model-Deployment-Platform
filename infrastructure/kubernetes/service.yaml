apiVersion: v1
kind: Service
metadata:
  name: ml-inference-service
  labels:
    app: ml-model-inference
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: ml-model-inference 